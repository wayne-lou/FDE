# Deployment

1. Copy `/demo_rc` to `C:\inetpub\demos\demo_rc`.
2. Configure Lucee datasource `demo_rc`.
3. Run PostgreSQL schema and seed SQL.
4. Browse `/demo_rc/index.cfm`.

For OpenAI mode, set `AI_MOCK_MODE=false` and `OPENAI_API_KEY` before restarting Lucee/Tomcat.
